<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Personal Productivity Hub</title>
    <style>
      :root {
        --bg-gradient: radial-gradient(circle at top left, #0f172a 0, #020617 45%, #111827 100%);
        --card-bg: rgba(15, 23, 42, 0.92);
        --accent: #22c55e;
        --accent-soft: rgba(34, 197, 94, 0.18);
        --accent-strong: rgba(34, 197, 94, 0.35);
      }

      body {
        font-family: system-ui, -apple-system, sans-serif;
        margin: 0;
        padding: 0;
        min-height: 100vh;
        color: #0f172a;
        background:
          radial-gradient(circle at 0% 0%, rgba(191, 219, 254, 0.55), transparent 55%),
          radial-gradient(circle at 100% 100%, rgba(187, 247, 208, 0.7), transparent 55%),
          linear-gradient(to bottom right, #eff6ff, #f9fafb);
        background-attachment: fixed;
      }

      header {
        background: linear-gradient(to right, rgba(15, 23, 42, 0.96), rgba(15, 23, 42, 0.88));
        backdrop-filter: blur(16px);
        border-bottom: 1px solid rgba(148, 163, 184, 0.25);
        color: white;
        padding: 0.85rem 1.75rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
        position: sticky;
        top: 0;
        z-index: 20;
      }

      main {
        padding: 2.5rem 1rem 3.5rem;
        max-width: 960px;
        margin: 0 auto;
      }

      .page-shell {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
      }

      .page-header {
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
      }

      .page-header--hero {
        padding: 1.3rem 1.2rem 1.1rem;
        border-radius: 1rem;
        background:
          radial-gradient(circle at 0 0, rgba(191, 219, 254, 0.55), transparent 60%),
          #ffffff;
        border: 1px solid rgba(148, 163, 184, 0.5);
        box-shadow: 0 20px 70px rgba(148, 163, 184, 0.55);
      }

      .page-kicker {
        font-size: 0.78rem;
        letter-spacing: 0.16em;
        text-transform: uppercase;
        color: #6b7280;
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        padding: 0.22rem 0.8rem;
        border-radius: 999px;
        background: #eff6ff;
        border: 1px solid rgba(148, 163, 184, 0.7);
      }

      .page-kicker-dot {
        width: 6px;
        height: 6px;
        border-radius: 999px;
        background: #22c55e;
      }

      .page-title {
        font-size: 1.6rem;
        font-weight: 600;
        letter-spacing: -0.02em;
        color: #0f172a;
        margin: 0;
      }

      .page-subtitle {
        font-size: 0.92rem;
        color: #6b7280;
        margin: 0;
      }

      .page-actions {
        margin-top: 0.75rem;
      }

      .page-card {
        border-radius: 1rem;
        padding: 1.3rem 1.2rem 1.1rem;
        background: #ffffff;
        border: 1px solid rgba(148, 163, 184, 0.4);
        box-shadow: 0 16px 55px rgba(148, 163, 184, 0.45);
      }

      .page-card + .page-card {
        margin-top: 1rem;
      }

      .page-section-title {
        font-size: 0.95rem;
        font-weight: 500;
        color: #111827;
        margin: 0 0 0.6rem;
      }

      .page-list {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
      }

      .page-list a {
        text-decoration: none;
      }

      .page-pill-link {
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        padding: 0.3rem 0.8rem;
        border-radius: 999px;
        background: #eff6ff;
        border: 1px solid rgba(148, 163, 184, 0.7);
        font-size: 0.82rem;
        color: #1f2937;
      }

      .page-pill-link strong {
        font-weight: 600;
      }

      .muted-text {
        font-size: 0.88rem;
        color: #6b7280;
      }

      a {
        color: #2563eb;
      }

      .nav-links {
        display: flex;
        align-items: center;
        gap: 0.5rem;
      }

      .nav-link-pill {
        display: inline-flex;
        align-items: center;
        gap: 0.3rem;
        padding: 0.26rem 0.85rem;
        border-radius: 999px;
        border: 1px solid rgba(148, 163, 184, 0.7);
        background: rgba(15, 23, 42, 0.05);
        color: #e5e7eb;
        font-size: 0.85rem;
        text-decoration: none;
      }

      .nav-link-pill--primary {
        border-color: rgba(34, 197, 94, 0.9);
        background: linear-gradient(135deg, #22c55e, #4ade80);
        color: #022c22;
      }

      .nav-link-pill span {
        font-weight: 500;
      }

      .nav-link-pill-dot {
        width: 6px;
        height: 6px;
        border-radius: 999px;
        background: rgba(148, 163, 184, 0.95);
      }

      .nav-link-pill--primary .nav-link-pill-dot {
        background: #16a34a;
      }

      form {
        margin: 0.5rem 0;
      }

      input,
      select,
      textarea {
        padding: 0.45rem 0.6rem;
        margin: 0.1rem 0;
        border-radius: 0.5rem;
        border: 1px solid rgba(148, 163, 184, 0.5);
        background: white;
        color: #0f172a;
        width: 100%;
        box-sizing: border-box;
      }

      input:focus,
      select:focus,
      textarea:focus {
        outline: 2px solid var(--accent-strong);
        border-color: var(--accent);
        box-shadow: 0 0 0 1px rgba(15, 23, 42, 0.9), 0 12px 35px rgba(15, 23, 42, 0.9);
      }

      button {
        border-radius: 999px;
        border: none;
        padding: 0.5rem 1.2rem;
        background: linear-gradient(135deg, #22c55e, #4ade80);
        color: #022c22;
        font-weight: 600;
        cursor: pointer;
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        box-shadow: 0 16px 45px rgba(22, 163, 74, 0.35);
        transition: transform 0.12s ease-out, box-shadow 0.15s ease-out, background 0.15s ease-out;
      }

      button:hover {
        transform: translateY(-1px) scale(1.01);
        box-shadow: 0 18px 55px rgba(22, 163, 74, 0.45);
        background: linear-gradient(135deg, #16a34a, #22c55e);
      }

      button:active {
        transform: translateY(0) scale(0.99);
        box-shadow: 0 10px 30px rgba(22, 163, 74, 0.3);
      }

      table {
        border-collapse: collapse;
        width: 100%;
        background: white;
        border-radius: 0.75rem;
        overflow: hidden;
      }

      th,
      td {
        border: 1px solid rgba(51, 65, 85, 0.85);
        padding: 0.6rem 0.75rem;
        text-align: left;
      }

      th {
        background: #eff6ff;
      }

      .auth-shell {
        min-height: calc(100vh - 64px);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2.5rem 1rem 3.5rem;
      }

      .auth-shell--login {
        position: relative;
        max-width: 980px;
        width: 100%;
      }

      .auth-grid {
        display: grid;
        grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
        gap: 2.8rem;
        max-width: 980px;
        width: 100%;
        align-items: stretch;
      }

      .auth-hero {
        position: relative;
        border-radius: 1.75rem;
        padding: 2.2rem 2.3rem;
        background:
          radial-gradient(circle at top left, rgba(191, 219, 254, 0.9), transparent 55%),
          linear-gradient(135deg, #eff6ff, #e0f2fe);
        border: 1px solid rgba(148, 163, 184, 0.5);
        box-shadow: 0 24px 80px rgba(148, 163, 184, 0.6);
        overflow: hidden;
      }

      .auth-hero--background {
        position: absolute;
        inset: 8% 10%;
        margin: 0 auto;
        opacity: 0.12;
        filter: blur(1.5px);
        pointer-events: none;
      }

      .auth-hero::before {
        content: "";
        position: absolute;
        inset: 0;
        background:
          radial-gradient(circle at 10% 0%, rgba(59, 130, 246, 0.16), transparent 55%),
          radial-gradient(circle at 80% 120%, rgba(34, 197, 94, 0.16), transparent 55%);
        opacity: 0.7;
        pointer-events: none;
      }

      .auth-hero-content {
        position: relative;
        z-index: 1;
      }

      .auth-kicker {
        font-size: 0.75rem;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        color: #4b5563;
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        background: rgba(255, 255, 255, 0.75);
        padding: 0.24rem 0.8rem;
        border-radius: 999px;
        border: 1px solid rgba(148, 163, 184, 0.8);
      }

      .auth-kicker-dot {
        width: 7px;
        height: 7px;
        border-radius: 999px;
        background: #22c55e;
        box-shadow: 0 0 0 4px rgba(34, 197, 94, 0.32);
      }

      .auth-title {
        font-size: 1.9rem;
        line-height: 1.25;
        margin: 1.1rem 0 0.6rem;
        color: #0f172a;
      }

      .auth-subtitle {
        font-size: 0.98rem;
        color: #4b5563;
        max-width: 30rem;
      }

      .auth-pills {
        display: flex;
        flex-wrap: wrap;
        gap: 0.4rem;
        margin-top: 1.3rem;
      }

      .auth-pill {
        font-size: 0.76rem;
        padding: 0.28rem 0.7rem;
        border-radius: 999px;
        border: 1px solid rgba(148, 163, 184, 0.55);
        background: rgba(255, 255, 255, 0.95);
        display: inline-flex;
        align-items: center;
        gap: 0.3rem;
        color: #111827;
      }

      .auth-pill-dot {
        width: 6px;
        height: 6px;
        border-radius: 999px;
        background: var(--accent);
      }

      .auth-stats {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 0.9rem;
        margin-top: 1.8rem;
      }

      .auth-stat {
        font-size: 0.78rem;
        padding: 0.7rem 0.75rem;
        border-radius: 0.9rem;
        background: #f9fafb;
        border: 1px solid rgba(148, 163, 184, 0.8);
        color: #111827;
      }

      .auth-stat-label {
        opacity: 0.86;
      }

      .auth-stat-value {
        font-size: 1.02rem;
        font-weight: 600;
        margin-top: 0.1rem;
        color: #111827;
      }

      .auth-quote {
        margin-top: 1.9rem;
        font-size: 0.85rem;
        color: #374151;
        display: flex;
        align-items: flex-start;
        gap: 0.6rem;
      }

      .auth-quote-mark {
        font-size: 1.8rem;
        line-height: 1;
        color: rgba(30, 64, 175, 0.8);
      }

      .auth-quote-meta {
        margin-top: 0.4rem;
        font-size: 0.78rem;
        color: #6b7280;
      }

      .auth-form-card {
        border-radius: 1.6rem;
        padding: 2.1rem 2.1rem 2rem;
        background:
          radial-gradient(circle at 0 0, rgba(191, 219, 254, 0.4), transparent 55%),
          #ffffff;
        border: 1px solid rgba(148, 163, 184, 0.5);
        box-shadow: 0 22px 70px rgba(148, 163, 184, 0.5);
        position: relative;
      }

      .auth-form-card--centered {
        max-width: 420px;
        width: 100%;
        z-index: 1;
      }

      .auth-form-card--login {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
      }

      .auth-login-brand {
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        font-size: 0.8rem;
        color: #6b7280;
        margin-bottom: 0.5rem;
      }

      .auth-login-brand-dot {
        width: 6px;
        height: 6px;
        border-radius: 999px;
        background: var(--accent);
      }

      .auth-form-header {
        margin-bottom: 1.4rem;
      }

      .auth-form-title {
        font-size: 1.3rem;
        font-weight: 600;
        color: #0f172a;
        margin: 0 0 0.18rem;
      }

      .auth-form-subtitle {
        font-size: 0.9rem;
        color: #6b7280;
      }

      .auth-field-group {
        margin-bottom: 0.9rem;
      }

      .auth-field-label {
        display: flex;
        justify-content: space-between;
        font-size: 0.84rem;
        margin-bottom: 0.2rem;
        color: #111827;
      }

      .auth-field-helper {
        font-size: 0.75rem;
        color: #6b7280;
      }

      .auth-actions {
        margin-top: 1.1rem;
        display: flex;
        flex-direction: column;
        gap: 0.8rem;
      }

      .auth-secondary-link {
        font-size: 0.85rem;
        color: #6b7280;
      }

      .auth-secondary-link a {
        color: #111827;
        font-weight: 500;
      }

      .auth-secondary-link a:hover {
        text-decoration: underline;
      }

      .auth-badge-row {
        margin-top: 1.2rem;
        display: flex;
        gap: 0.5rem;
        flex-wrap: wrap;
        font-size: 0.75rem;
        color: #6b7280;
      }

      .auth-badge {
        padding: 0.22rem 0.7rem;
        border-radius: 999px;
        border: 1px dashed rgba(148, 163, 184, 0.7);
        background: #f9fafb;
      }

      .auth-form-aside {
        margin-top: 1.1rem;
        display: flex;
        flex-direction: column;
        gap: 0.4rem;
        font-size: 0.8rem;
        color: #9ca3af;
      }

      .auth-form-aside-note {
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        max-width: 18rem;
      }

      .auth-form-aside-note::before {
        content: "";
        width: 6px;
        height: 6px;
        border-radius: 999px;
        background: var(--accent);
        opacity: 0.7;
      }

      .pill-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        padding: 0.2rem 0.65rem;
        border-radius: 999px;
        background: rgba(15, 23, 42, 0.92);
        border: 1px solid rgba(148, 163, 184, 0.6);
        font-size: 0.78rem;
      }

      .pill-dot {
        width: 6px;
        height: 6px;
        border-radius: 999px;
        background: var(--accent);
      }

      @media (max-width: 880px) {
        header {
          padding-inline: 1.1rem;
        }

        /* On smaller screens, keep the login form primary and tuck hero details away */
        .auth-hero--background .auth-stats,
        .auth-hero--background .auth-quote {
          display: none;
        }

        main {
          padding-top: 1.5rem;
        }

        .auth-shell {
          padding-top: 1.4rem;
          padding-bottom: 2.6rem;
        }

        .auth-grid {
          grid-template-columns: minmax(0, 1fr);
          max-width: 520px;
        }

        .auth-grid--login .auth-form-card {
          order: -1;
        }

        .auth-grid--login .auth-hero {
          order: 0;
        }

        .auth-hero {
          padding: 1.8rem 1.7rem;
        }

        .auth-form-card {
          padding: 1.7rem 1.6rem 1.6rem;
        }
      }

      @media (max-width: 520px) {
        header {
          padding-inline: 1rem;
        }

        .auth-hero {
          padding: 1.6rem 1.3rem;
        }

        .auth-title {
          font-size: 1.7rem;
        }

        .auth-grid {
          gap: 1.8rem;
        }

        .auth-form-card {
          padding: 1.5rem 1.3rem 1.4rem;
        }
      }
    </style>
  </head>
  <body>
    <header>
      <div>
        <a href="/" style="color:white; text-decoration:none; font-weight:bold;">Productivity Hub</a>
      </div>
      <div class="nav-links">
        <% if (typeof user !== 'undefined' && user) { %>
          <a class="nav-link-pill" href="/tasks">
            <span class="nav-link-pill-dot"></span>
            <span>Tasks</span>
          </a>
          <a class="nav-link-pill" href="/search">
            <span class="nav-link-pill-dot"></span>
            <span>Search</span>
          </a>
          <form action="/auth/logout" method="post" style="display:inline; margin-left: 0.2rem;">
            <button class="nav-link-pill nav-link-pill--primary" type="submit">
              <span class="nav-link-pill-dot"></span>
              <span>Logout (<%= user.email %>)</span>
            </button>
          </form>
        <% } else { %>
          <a class="nav-link-pill" href="/auth/login">
            <span class="nav-link-pill-dot"></span>
            <span>Login</span>
          </a>
          <a class="nav-link-pill nav-link-pill--primary" href="/auth/signup">
            <span class="nav-link-pill-dot"></span>
            <span>Sign up</span>
          </a>
        <% } %>
      </div>
    </header>
    <main>
      <%- body %>
    </main>
  </body>
</html>
