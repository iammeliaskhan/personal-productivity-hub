<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Personal Productivity Hub</title>
    <style>
      body { font-family: system-ui, -apple-system, sans-serif; margin: 0; padding: 0; }
      header { background: #111827; color: white; padding: 1rem; display: flex; justify-content: space-between; align-items: center; }
      main { padding: 1rem; max-width: 900px; margin: 0 auto; }
      a { color: #2563eb; }
      .nav-links a { margin-right: 1rem; }
      form { margin: 0.5rem 0; }
      input, select, textarea { padding: 0.25rem 0.4rem; margin: 0.1rem 0; }
      table { border-collapse: collapse; width: 100%; }
      th, td { border: 1px solid #e5e7eb; padding: 0.4rem; text-align: left; }
    </style>
  </head>
  <body>
    <header>
      <div>
        <a href="/" style="color:white; text-decoration:none; font-weight:bold;">Productivity Hub</a>
      </div>
      <div class="nav-links">
        <% if (typeof user !== 'undefined' && user) { %>
          <a href="/tasks">Tasks</a>
          <a href="/search">Search</a>
          <form action="/auth/logout" method="post" style="display:inline;">
            <button type="submit">Logout (<%= user.email %>)</button>
          </form>
        <% } else { %>
          <a href="/auth/login">Login</a>
          <a href="/auth/signup">Sign up</a>
        <% } %>
      </div>
    </header>
    <main>
      <%- body %>
    </main>
  </body>
</html>
