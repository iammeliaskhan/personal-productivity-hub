<div class="auth-shell auth-shell--login">
  <section class="auth-form-card auth-form-card--centered auth-form-card--login">
    <div class="auth-login-brand">
      <span class="auth-login-brand-dot"></span>
      <span>Personal Productivity Hub</span>
    </div>

    <div class="auth-form-header">
      <h2 class="auth-form-title">Welcome back</h2>
      <p class="auth-form-subtitle">Log in to pick up where you left off.</p>
    </div>

    <form method="post" action="/auth/login">
      <div class="auth-field-group">
        <div class="auth-field-label">
          <label for="email">Email</label>
        </div>
        <input id="email" type="email" name="email" autocomplete="email" required />
      </div>

      <div class="auth-field-group">
        <div class="auth-field-label">
          <label for="password">Password</label>
          <span class="auth-field-helper">Keep it private. We keep it encrypted.</span>
        </div>
        <input
          id="password"
          type="password"
          name="password"
          autocomplete="current-password"
          required
        />
      </div>

      <div class="auth-actions">
        <button type="submit">
          Log into your workspace
        </button>
        <div class="auth-secondary-link">
          New here?
          <a href="/auth/signup">Create an account</a>
        </div>
      </div>
    </form>

    <div class="auth-badge-row">
      <div class="auth-badge">Built for deep work & realistic planning.</div>
      <div class="auth-badge">Your data stays in your workspace.</div>
    </div>

    <div class="auth-form-aside">
      <div class="auth-form-aside-note">
        Take 30 seconds to choose today&#39;s top three, then get back to real work.
      </div>
      <div class="auth-form-aside-note">
        No streaks, no pressure&mdash;just a quiet place to move things forward.
      </div>
    </div>
  </section>
</div>
